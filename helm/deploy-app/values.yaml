app:
  name: app
  replicaCount: 1
  container:
    image: add-image-here
    pullPolicy: IfNotPresent
    port: 80
    config: []
    env:
       - key: key
         value: value
  service:
    type: ClusterIP
    port: 80
